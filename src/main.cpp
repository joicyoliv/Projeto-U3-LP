#include "petfera.h"

int main(){
	
	PetFera loja;
	loja.menu();
	//shared_ptr<Veterinario>  _vet = make_shared<Veterinario>(5, "string _nome", "string _cpf", 10, 5, 'a',"string _especialidade");

	return 0;
}